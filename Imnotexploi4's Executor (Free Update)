local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Create GUI Elements
local screenGui = Instance.new("ScreenGui")
local mainFrame = Instance.new("Frame")
local keyFrame = Instance.new("Frame")

-- Key System GUI Setup
keyFrame.Name = "KeyFrame"
keyFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
keyFrame.Size = UDim2.new(0, 400, 0, 200)
keyFrame.Position = UDim2.new(0.5, -200, 0.5, -100)
keyFrame.Visible = true
keyFrame.Parent = screenGui

local keyFrameCorner = Instance.new("UICorner")
keyFrameCorner.CornerRadius = UDim.new(0, 20)
keyFrameCorner.Parent = keyFrame

local keyTitleLabel = Instance.new("TextLabel")
keyTitleLabel.Name = "KeyTitleLabel"
keyTitleLabel.Size = UDim2.new(1, -20, 0, 40)
keyTitleLabel.Position = UDim2.new(0, 10, 0, 10)
keyTitleLabel.BackgroundTransparency = 1
keyTitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
keyTitleLabel.Font = Enum.Font.GothamBold
keyTitleLabel.TextSize = 24
keyTitleLabel.Text = "Enter Your Key"
keyTitleLabel.Parent = keyFrame

local keyInput = Instance.new("TextBox")
keyInput.Name = "KeyInput"
keyInput.Size = UDim2.new(1, -40, 0, 50)
keyInput.Position = UDim2.new(0, 20, 0, 60)
keyInput.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
keyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
keyInput.Text = ""
keyInput.ClearTextOnFocus = false
keyInput.Font = Enum.Font.Code
keyInput.TextSize = 18
keyInput.PlaceholderText = "put the key here"
keyInput.Parent = keyFrame

local keyButton = Instance.new("TextButton")
keyButton.Name = "KeyButton"
keyButton.BackgroundColor3 = Color3.fromRGB(0, 191, 255)
keyButton.Text = "Check Key"
keyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
keyButton.Font = Enum.Font.Gotham
keyButton.TextSize = 18
keyButton.Size = UDim2.new(0, 150, 0, 50)
keyButton.Position = UDim2.new(0.5, -75, 0, 120)
keyButton.Parent = keyFrame

local keyButtonCorner = Instance.new("UICorner")
keyButtonCorner.CornerRadius = UDim.new(0, 12)
keyButtonCorner.Parent = keyButton

-- Main Executor GUI Setup
screenGui.Name = "ExecutorGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = PlayerGui

mainFrame.Name = "MainFrame"
mainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
mainFrame.Size = UDim2.new(0, 600, 0, 700)
mainFrame.Position = UDim2.new(0.5, -300, 0.5, -350)
mainFrame.Visible = false
mainFrame.Parent = screenGui

local mainFrameCorner = Instance.new("UICorner")
mainFrameCorner.CornerRadius = UDim.new(0, 20)
mainFrameCorner.Parent = mainFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "TitleLabel"
titleLabel.Size = UDim2.new(1, -20, 0, 60)
titleLabel.Position = UDim2.new(0, 10, 0, 10)
titleLabel.BackgroundTransparency = 1
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 30
titleLabel.Text = "Imnotexploi4's Executor V1.4"
titleLabel.Parent = mainFrame

local codeInput = Instance.new("TextBox")
codeInput.Name = "CodeInput"
codeInput.Size = UDim2.new(1, -40, 0, 350)
codeInput.Position = UDim2.new(0, 20, 0, 80)
codeInput.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
codeInput.TextColor3 = Color3.fromRGB(255, 255, 255)
codeInput.Text = ""
codeInput.ClearTextOnFocus = false
codeInput.MultiLine = true
codeInput.TextWrapped = true
codeInput.Font = Enum.Font.Code
codeInput.TextSize = 20
codeInput.PlaceholderText = "Enter your script here..."
codeInput.Parent = mainFrame

local inputCorner = Instance.new("UICorner")
inputCorner.CornerRadius = UDim.new(0, 15)
inputCorner.Parent = codeInput

local function createButton(name, text, position, size, color, hoverColor)
local button = Instance.new("TextButton")
button.Name = name
button.BackgroundColor3 = color
button.Text = text
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.Font = Enum.Font.Gotham
button.TextSize = 18
button.Position = position
button.Size = size button.Parent = mainFrame

local buttonCorner = Instance.new("UICorner")
buttonCorner.CornerRadius = UDim.new(0, 12)
buttonCorner.Parent = button

local function onMouseEnter()
button.BackgroundColor3 = hoverColor
end

local function onMouseLeave()
button.BackgroundColor3 = color
end

button.MouseEnter:Connect(onMouseEnter)
button.MouseLeave:Connect(onMouseLeave)

return button
end

local executeButton = createButton("ExecuteButton", "Execute", UDim2.new(0, 20, 1, -170), UDim2.new(0, 180, 0, 50), Color3.fromRGB(0, 128, 0), Color3.fromRGB(0, 100, 0))
local saveButton = createButton("SaveButton", "Save Script", UDim2.new(0.5, -90, 1, -170), UDim2.new(0, 180, 0, 50), Color3.fromRGB(0, 191, 255), Color3.fromRGB(0, 140, 210))
local copyButton = createButton("CopyButton", "Copy", UDim2.new(1, -200, 1, -170), UDim2.new(0, 180, 0, 50), Color3.fromRGB(255, 165, 0), Color3.fromRGB(204, 130, 0))
local clearButton = createButton("ClearButton", "Clear", UDim2.new(0, 20, 1, -240), UDim2.new(0, 180, 0, 50), Color3.fromRGB(255, 69, 0), Color3.fromRGB(204, 50, 0))
local loadButton = createButton("LoadButton", "Load Script", UDim2.new(1, -200, 1, -240), UDim2.new(0, 180, 0, 50), Color3.fromRGB(112, 128, 144), Color3.fromRGB(80, 100, 114))

local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Size = UDim2.new(0, 40, 0, 40)
closeButton.Position = UDim2.new(1, -50, 0, 10)
closeButton.BackgroundColor3 = Color3.fromRGB(220, 20, 60)
closeButton.Text = "X"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 18
closeButton.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 15)
closeCorner.Parent = closeButton

local errorLabel = Instance.new("TextLabel")
errorLabel.Name = "ErrorLabel"
errorLabel.Size = UDim2.new(1, -20, 0, 40)
errorLabel.Position = UDim2.new(0, 10, 1, -80)
errorLabel.BackgroundTransparency = 1 errorLabel.TextColor3 = Color3.fromRGB(255, 69, 0)
errorLabel.Font = Enum.Font.GothamBold
errorLabel.TextSize = 18
errorLabel.Text = ""
errorLabel.Visible = false
errorLabel.Parent = mainFrame

local toggleButton = Instance.new("ImageButton")
toggleButton.Name = "ToggleButton"
toggleButton.Size = UDim2.new(0, 50, 0, 50)
toggleButton.Position = UDim2.new(0, 10, 0, 10)
toggleButton.Image = "rbxassetid://111767375753277" -- Imnotexploi4 icon
toggleButton.Visible = false
toggleButton.Parent = screenGui
toggleButton.ZIndex = 10

local function makeDraggable(guiElement)
local dragging = false
local dragInput
local dragStart
local startPos

local function update(input)
local delta = input.Position - dragStart
guiElement.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

guiElement.InputBegan:Connect(function(input)
if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
dragging = true
dragStart = input.Position
startPos = guiElement.Position

input.Changed:Connect(function()
if input.UserInputState == Enum.UserInputState.End then
dragging = false
end
end)
end
end)

guiElement.InputChanged:Connect(function(input)
if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
dragInput = input
end
end)

UserInputService.InputChanged:Connect(function(input)
if input == dragInput and dragging then
update(input)
end
end)
end

makeDraggable(mainFrame)
makeDraggable(keyFrame)
makeDraggable(toggleButton)

local function toggleGUI()
mainFrame.Visible = not mainFrame.Visible
toggleButton.Visible = not mainFrame.Visible
end

closeButton.MouseButton1Click:Connect(toggleGUI)
toggleButton.MouseButton1Click:Connect(toggleGUI)

local function executeCode()
local code = codeInput.Text

local function safeExecute(source)
local func, err = loadstring(source)
if func then
setfenv(func, getfenv()) local success, execErr = pcall(func)
if not success then
errorLabel.Text = "Error executing code: " .. execErr
errorLabel.Visible = true
else
errorLabel.Text = ""
errorLabel.Visible = false
end
else
errorLabel.Text = "Error parsing code: " .. err
errorLabel.Visible = true
end
end

safeExecute(code)
end

local function copyCode()
if setclipboard then
setclipboard(codeInput.Text)
errorLabel.Text = "Code copied to clipboard"
errorLabel.Visible = true
else
errorLabel.Text = "setclipboard function is not available."
errorLabel.Visible = true
end
end

local function clearCode()
codeInput.Text = ""
errorLabel.Text = "Code input cleared"
errorLabel.Visible = true
end

local function saveCode()
errorLabel.Text = "Save functionality not implemented"
errorLabel.Visible = true
end

local function loadCode()
local loadedCode = "print('Hello, world!')" -- Replace with actual loaded content
codeInput.Text = loadedCode
errorLabel.Text = "Code loaded"
errorLabel.Visible = true
end

executeButton.MouseButton1Click:Connect(executeCode)
copyButton.MouseButton1Click:Connect(copyCode)
saveButton.MouseButton1Click:Connect(saveCode)
clearButton.MouseButton1Click:Connect(clearCode)
loadButton.MouseButton1Click:Connect(loadCode)

local function adjustGUISize()
local screenSize = workspace.CurrentCamera.ViewportSize
if screenSize.Y <= 720 then
mainFrame.Size = UDim2.new(0, 340, 0, 500)
mainFrame.Position = UDim2.new(0.5, -170, 0.5, -250)
titleLabel.Size = UDim2.new(1, -20, 0, 50)
titleLabel.Position = UDim2.new(0, 10, 0, 10)
codeInput.Size = UDim2.new(1, -20, 0, 200)
codeInput.Position = UDim2.new(0, 10, 0, 70)

for _, button in ipairs(mainFrame:GetChildren()) do
if button:IsA("TextButton") and button.Name ~= "CloseButton" then
button.Size = UDim2.new(0, 100, 0, 30)
end
end

executeButton.Position = UDim2.new(0, 10, 1, -90)
saveButton.Position = UDim2.new(0.33, 0, 1, -90)
copyButton.Position = UDim2.new(0.66, 0, 1, -90)
clearButton.Position = UDim2.new(0, 10, 1, -130) loadButton.Position = UDim2.new(0.33, 0, 1, -130)
errorLabel.Position = UDim2.new(0, 10, 1, -40)
else
mainFrame.Size = UDim2.new(0, 600, 0, 700)
mainFrame.Position = UDim2.new(0.5, -300, 0.5, -350)
titleLabel.Size = UDim2.new(1, -20, 0, 60)
titleLabel.Position = UDim2.new(0, 10, 0, 10)
codeInput.Size = UDim2.new(1, -40, 0, 350)
codeInput.Position = UDim2.new(0, 20, 0, 80)

for _, button in ipairs(mainFrame:GetChildren()) do
if button:IsA("TextButton") and button.Name ~= "CloseButton" then
button.Size = UDim2.new(0, 180, 0, 50)
end
end

executeButton.Position = UDim2.new(0, 20, 1, -170)
saveButton.Position = UDim2.new(0.5, -90, 1, -170)
copyButton.Position = UDim2.new(1, -200, 1, -170)
clearButton.Position = UDim2.new(0, 20, 1, -240)
loadButton.Position = UDim2.new(1, -200, 1, -240)
errorLabel.Position = UDim2.new(0, 10, 1, -80)
end
end

adjustGUISize()
workspace.CurrentCamera:GetPropertyChangedSignal("ViewportSize"):Connect(adjustGUISize)

local function checkKey()
if keyInput.Text == "imnotexploi4-executor" then
errorLabel.Text = "Key correct"
errorLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
errorLabel.Visible = true
keyFrame.Visible = false
mainFrame.Visible = true
else
errorLabel.Text = "Key incorrect. Please try again"
errorLabel.TextColor3 = Color3.fromRGB(255, 69, 0)
errorLabel.Visible = true
end
end

keyButton.MouseButton1Click:Connect(checkKey)

print("Hello " .. LocalPlayer.Name .. ", Welcome to Imnotexploi4's Executor V1.4")
errorLabel.Visible = false
